# -------------------------------------------------------------------------------
# Dev Environment Services - Docker Compose
#
# Author: Alex Freidah
#
# Provides MinIO (x3), PostgreSQL, and observability stack for local
# development and demos. Integration tests use testcontainers instead — see
# internal/integration/helpers_test.go.
#
# The MinIO images come from quay.io at a pinned release. Docker Hub stopped
# serving minio/minio and minio/mc to anonymous pulls, which leaves a machine
# holding a cached copy working while a clean one cannot start the stack.
#
# Usage: make dev-deps        (or: docker compose -f docker-compose.test.yml up -d --wait)
# Cleanup: make dev-clean     (or: docker compose -f docker-compose.test.yml down -v)
# -------------------------------------------------------------------------------

services:
  minio-1:
    image: quay.io/minio/minio:RELEASE.2025-04-22T22-12-26Z
    command: server /data --console-address ":9001"
    ports:
      - "19000:9000"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio-1-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 2s
      timeout: 5s
      retries: 5

  minio-2:
    image: quay.io/minio/minio:RELEASE.2025-04-22T22-12-26Z
    command: server /data --console-address ":9001"
    ports:
      - "19002:9000"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio-2-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 2s
      timeout: 5s
      retries: 5

  minio-3:
    image: quay.io/minio/minio:RELEASE.2025-04-22T22-12-26Z
    command: server /data --console-address ":9001"
    ports:
      - "19004:9000"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio-3-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 2s
      timeout: 5s
      retries: 5

  postgres:
    # Debian rather than alpine: musl has no locale data, so text sorts by byte
    # regardless of collation and the stack under test stops resembling a real
    # deployment.
    image: postgres:16
    command:
      - postgres
      - -c
      - max_connections=300
    ports:
      - "15432:5432"
    environment:
      POSTGRES_DB: s3proxy_test
      POSTGRES_USER: s3proxy
      # NOSONAR — test-only docker-compose; matches the user name to keep the
      # integration harness bootable without environment variables. Never used
      # in production.
      POSTGRES_PASSWORD: s3proxy
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U s3proxy -d s3proxy_test"]
      interval: 2s
      timeout: 5s
      retries: 5

  minio-setup:
    image: quay.io/minio/mc:RELEASE.2025-04-16T18-13-26Z
    depends_on:
      minio-1:
        condition: service_healthy
      minio-2:
        condition: service_healthy
      minio-3:
        condition: service_healthy
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        mc alias set minio1 http://minio-1:9000 minioadmin minioadmin &&
        mc alias set minio2 http://minio-2:9000 minioadmin minioadmin &&
        mc alias set minio3 http://minio-3:9000 minioadmin minioadmin &&
        mc mb --ignore-existing minio1/backend1 &&
        mc mb --ignore-existing minio2/backend2 &&
        mc mb --ignore-existing minio3/backend3

  tempo:
    image: grafana/tempo:2.9.1
    network_mode: host
    volumes:
      - ./deploy/monitoring/tempo.yml:/etc/tempo.yml:ro
    command: ["-config.file=/etc/tempo.yml"]
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3200/ready"]
      interval: 5s
      timeout: 3s
      retries: 5

  loki:
    image: grafana/loki:3.4.2
    user: "0"
    network_mode: host
    volumes:
      - ./deploy/monitoring/loki.yml:/etc/loki/config.yml:ro
      - loki-data:/var/loki
    command: ["-config.file=/etc/loki/config.yml"]
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3100/ready"]
      interval: 5s
      timeout: 3s
      retries: 5

  alloy:
    image: grafana/alloy:latest
    volumes:
      - ./deploy/monitoring/alloy.alloy:/etc/alloy/config.alloy:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: ["run", "/etc/alloy/config.alloy"]
    depends_on:
      loki:
        condition: service_healthy

  prometheus:
    image: prom/prometheus:latest
    network_mode: host
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--web.listen-address=:19090"
      - "--web.enable-remote-write-receiver"
    volumes:
      - ./deploy/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    depends_on:
      minio-setup:
        condition: service_completed_successfully

  grafana:
    image: grafana/grafana:latest
    ports:
      - "13000:3000"
    environment:
      GF_AUTH_ANONYMOUS_ENABLED: "true"
      GF_AUTH_ANONYMOUS_ORG_ROLE: Admin
      GF_AUTH_DISABLE_LOGIN_FORM: "true"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./deploy/monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/s3-orchestrator.json:/var/lib/grafana/dashboards/s3-orchestrator.json:ro
    depends_on:
      prometheus:
        condition: service_started
      tempo:
        condition: service_healthy
      loki:
        condition: service_healthy

volumes:
  minio-1-data:
  minio-2-data:
  minio-3-data:
  loki-data:
